<!DOCTYPE html>
<html>
  <head>
    <title>PhotoApp</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
    <% if user_signed_in? %>
      <li>
        <ul><%= link_to 'Profile', profile_path(current_user) %></ul>
        <ul><%= link_to 'Sign out', destroy_user_session_path, method: :delete %></ul>
        <ul><%= link_to 'Images', images_path %></ul>
      </li>
    <% else %>
      <li>
        <ul><%= link_to 'Sign in', user_session_path %></ul>
        <ul><%= link_to 'Sign up', new_user_session_path %></ul>
      </li>
    <% end %>
    <div>
      <p class="notice"><%= notice %></p>
      <p class="alert"><%= alert %></p>
    </div>
    <%= yield %>
  </body>
</html>
